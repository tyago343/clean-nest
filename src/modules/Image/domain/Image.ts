class Image {
  public id: string;
  public name: string;
  public url: string;

  constructor(id: string, name: string, url: string) {
    this.id = id;
    this.name = name;
    this.url = url;
  }
}
export default Image;
